import React, { useState } from 'react';

const PrintToPDF = () => {
  const [state, setstate] = useState({
    numOfPages: null,
    pageNumber: 1
  });
  return <div></div>;
};

export default PrintToPDF;
